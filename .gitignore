.DS_Store
*.swp
*.stow

# Lokale Env-Overrides (geräte-/situationsspezifisch)
env.*.local

# Entschlüsselte Secrets (Plaintext – nie committen!)
# Die verschlüsselte .age-Version ist bewusst im Repo.
secrets.sh
*.dec
*.key

# lokale Backups & temporäre Daten
.backup/
*.bak*
