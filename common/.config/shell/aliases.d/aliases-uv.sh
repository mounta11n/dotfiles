

# ——— UV - Python Projects ————————————
alias uinit='uv init'
alias uadd='uv add'
alias uremove='uv remove'
alias usync='uv sync'
alias ulock='uv lock'
alias urun='uv run'
alias utree='uv tree'
alias ubuild='uv build'
alias upublish='uv publish'

# ——— UV - Python Versions ————————————
alias upy='uv python'
alias upy-install='uv python install'
alias upy-list='uv python list'
alias upy-find='uv python find'
alias upy-pin='uv python pin'
alias upy-uninstall='uv python uninstall'

# ——— UV - Python Tools ———————————————
alias utool='uv tool'
alias utool-install='uv tool install'
alias utool-uninstall='uv tool uninstall'
alias utool-list='uv tool list'
alias utool-update-sh='uv tool update-shell'

# ——— UV - PIP Interface ——————————————
alias uvenv='uv venv'
alias uvenva='uv venv && source ./.venv/bin/activate'
alias uva='uv venv && source ./.venv/bin/activate'
alias upip='uv pip'
alias upip-install='uv pip install'
alias upip-show='uv pip show'
alias upip-freeze='uv pip freeze'
alias upip-check='uv pip check'
alias upip-list='uv pip list'
alias upip-uninstall='uv pip uninstall'
alias upip-tree='uv pip tree'
alias upip-compile='uv pip compile'
alias upip-sync='uv pip sync'

# ——— UV - Utilities ——————————————————
alias ucache-clean='uv cache clean'
alias ucache-prune='uv cache prune'
alias ucache-dir='uv cache dir'
alias utool-dir='uv tool dir'
alias upy-dir='uv python dir'
alias uself-update='uv self update'
alias uv-self-update='uv self update'
alias upipx='uvx'
alias ux='uvx'

# ——— uv ——————————————————————————————
alias upip='uv pip'
alias uv-install='uv pip install'
alias uv-compile='uv pip compile'
alias uv-sync='uv pip sync'
alias unv='uv venv'
alias upipx='uvx'

